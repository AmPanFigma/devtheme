<section class="static-page">
    <div class="container-fluid">
      <div class="page-title">
        <h1>{{ page.title }}</h1>
      </div>
      <div class="page-content">
        {{ page.content }}
      </div>
    </div>
  </section>