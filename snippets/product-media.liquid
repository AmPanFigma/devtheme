{% case media.media_type %}
  {% when 'image' %}
    <img
      src="{{ media | img_url: 'large' }}"
      alt="{{ media.preview_image.alt }}"
      class="img ab">

  {% when 'external_video' %}
    {{ media | external_video_tag: class: 'img ab' }}

  {% when 'video' %}
    {{ media | video_tag: controls: true, class: 'img ab' }}

  {% when 'model' %}
    {{ media | model_viewer_tag }}

  {% else %}
    {{ media | media_tag }}

{% endcase %}